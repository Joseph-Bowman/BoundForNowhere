using UnityEngine;

public enum Level
{
    //Level defines the difficulty of the stage with level1 being the starter levels and level 4 being end game difficulty.
    start,
    end,
    level1,
    level2,
    level3,
    level4
}
